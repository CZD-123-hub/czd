# 智能编程学习助手｜课设提交整合说明

## 1. 项目概述

本项目为一个面向编程学习场景的全栈系统，核心目标是将**智能问答（RAG）**、**知识图谱导航**、**学习路径规划**与**代码片段复用**整合到同一平台，帮助学习者完成“提问-理解-实践-复盘”的学习闭环。

项目名称：智能编程学习助手  
后端：Spring Boot 3.2.5 + MyBatis-Plus + Spring Security + Redis + Neo4j + Elasticsearch  
前端：Vue 3 + TypeScript + Vite + Element Plus + Pinia + ECharts + Monaco Editor  
数据库：MySQL 8.0（关系数据）+ Neo4j 5（图谱）+ Redis 7（缓存）+ Elasticsearch 8（向量检索）

---

## 2. 系统功能清单（按模块）

### 2.1 用户与认证模块

- 用户注册
- 用户登录
- JWT 鉴权
- 获取当前用户信息
- 个人资料摘要
- 头像上传

### 2.2 智能问答模块

- 多轮会话管理（会话列表、消息列表、删除会话）
- SSE 流式回答
- 回答反馈（useful/useless）
- 检索评估指标查询与重置（retrieval metrics）
- 上下文缓存与问答缓存

### 2.3 RAG 检索模块

- QueryRewrite 查询扩展
- Elasticsearch 向量检索（文档级+知识块级）
- 本地 embedding 相似度兜底检索
- 关键词检索（BM25 风格融合）
- 混合召回与重排序
- 图谱关联增强（文档关联节点/节点关联文档）

### 2.4 知识图谱模块

- 图谱总览
- 节点详情查询
- 邻居展开（按深度）
- 关键词图谱搜索
- 图谱健康度检查
- 节点关联文档查询

### 2.5 学习路径模块

- 输入学习目标生成路径
- 路径列表查询
- 路径节点状态更新（todo/doing/done）
- 路径删除

### 2.6 代码片段模块

- 代码片段增删改查
- 代码片段推荐（会话上下文相关）
- 片段使用计数（mark used）
- 片段反馈
- 片段导出（JSON）
- 片段导入（JSON）

### 2.7 学习分析模块

- 仪表盘统计
- 智能洞察
- 周计划勾选
- 学习热力图
- 知识雷达图
- PDF 学习报告导出

### 2.8 知识文档模块

- 文档列表查询
- 文档新增
- 文档删除
- 文档关联图谱节点查询

---

## 3. 用户角色与用例

当前实现以学习者为中心，角色简化为两类：

- 未登录用户：可访问登录/注册页面
- 已登录学习者用户：可访问系统全部核心功能

### 3.1 用例列表（仅列名称）

- 用户注册
- 用户登录
- 查看个人资料
- 上传头像
- 发起问答
- 流式接收答案
- 查看会话历史
- 删除会话
- 提交回答反馈
- 查看检索评估
- 重置检索评估
- 浏览知识图谱
- 搜索图谱节点
- 查看图谱节点详情
- 展开邻居节点
- 查看图谱健康度
- 生成学习路径
- 更新学习路径节点状态
- 删除学习路径
- 新建代码片段
- 编辑代码片段
- 删除代码片段
- 搜索代码片段
- 获取代码推荐
- 导出代码片段
- 导入代码片段
- 查看知识文档
- 新增知识文档
- 删除知识文档
- 查看文档关联节点
- 查看学习仪表盘
- 查看学习热力图
- 查看知识雷达图
- 查看智能洞察
- 导出学习报告

---

## 4. 数据库表结构（MySQL）

### 4.1 核心表

- `user`
- `conversation`
- `message`
- `feedback`
- `code_snippet`
- `learning_path`
- `learning_node`
- `learning_record`
- `knowledge_document`
- `knowledge_chunk`

### 4.2 表关系说明

- `user` 1:N `conversation`
- `conversation` 1:N `message`
- `message` 1:N `feedback`（按 message_id + user_id 去重）
- `user` 1:N `code_snippet`
- `user` 1:N `learning_path`
- `learning_path` 1:N `learning_node`
- `user` 1:N `learning_record`
- `knowledge_document` 1:N `knowledge_chunk`

### 4.3 建表 SQL（来自 `backend/src/main/resources/schema.sql`，已去除测试数据）

```sql
CREATE DATABASE IF NOT EXISTS coding_assistant DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE coding_assistant;

CREATE TABLE IF NOT EXISTS `user` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `username` VARCHAR(50) NOT NULL UNIQUE,
    `password` VARCHAR(255) NOT NULL,
    `email` VARCHAR(100) NOT NULL UNIQUE,
    `avatar` VARCHAR(255) DEFAULT NULL,
    `level` VARCHAR(20) DEFAULT 'beginner',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_user_username` (`username`),
    INDEX `idx_user_email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `conversation` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `user_id` BIGINT NOT NULL,
    `title` VARCHAR(200) DEFAULT NULL,
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_conversation_user_id` (`user_id`),
    CONSTRAINT `fk_conversation_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `message` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `conversation_id` BIGINT NOT NULL,
    `role` VARCHAR(20) NOT NULL,
    `content` TEXT NOT NULL,
    `sources` JSON DEFAULT NULL,
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_message_conversation_id` (`conversation_id`),
    CONSTRAINT `fk_message_conversation` FOREIGN KEY (`conversation_id`) REFERENCES `conversation` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `feedback` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `message_id` BIGINT NOT NULL,
    `user_id` BIGINT NOT NULL,
    `rating` VARCHAR(10) NOT NULL,
    `comment` TEXT DEFAULT NULL,
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_feedback_message_user` (`message_id`, `user_id`),
    CONSTRAINT `fk_feedback_message` FOREIGN KEY (`message_id`) REFERENCES `message` (`id`) ON DELETE CASCADE,
    CONSTRAINT `fk_feedback_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `code_snippet` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `user_id` BIGINT NOT NULL,
    `title` VARCHAR(200) NOT NULL,
    `code` TEXT NOT NULL,
    `language` VARCHAR(30) NOT NULL,
    `description` TEXT DEFAULT NULL,
    `tags` JSON DEFAULT NULL,
    `use_count` INT DEFAULT 0,
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_snippet_user_id` (`user_id`),
    CONSTRAINT `fk_snippet_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `learning_path` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `user_id` BIGINT NOT NULL,
    `target` VARCHAR(200) NOT NULL,
    `status` VARCHAR(20) DEFAULT 'active',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_path_user_id` (`user_id`),
    CONSTRAINT `fk_path_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `learning_node` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `path_id` BIGINT NOT NULL,
    `knowledge_id` VARCHAR(100) NOT NULL,
    `node_order` INT NOT NULL,
    `status` VARCHAR(20) DEFAULT 'todo',
    `resource_urls` JSON DEFAULT NULL,
    INDEX `idx_node_path_id` (`path_id`),
    CONSTRAINT `fk_node_path` FOREIGN KEY (`path_id`) REFERENCES `learning_path` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `learning_record` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `user_id` BIGINT NOT NULL,
    `action_type` VARCHAR(30) NOT NULL,
    `target_id` VARCHAR(100) DEFAULT NULL,
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_record_user_created` (`user_id`, `created_at`),
    CONSTRAINT `fk_record_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `knowledge_document` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `title` VARCHAR(200) NOT NULL,
    `content` MEDIUMTEXT NOT NULL,
    `category` VARCHAR(50) DEFAULT NULL,
    `embedding` JSON DEFAULT NULL,
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_doc_category` (`category`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `knowledge_chunk` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `document_id` BIGINT NOT NULL,
    `chunk_index` INT NOT NULL,
    `content` TEXT NOT NULL,
    `embedding` JSON DEFAULT NULL,
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_chunk_doc_idx` (`document_id`, `chunk_index`),
    INDEX `idx_chunk_document_id` (`document_id`),
    CONSTRAINT `fk_chunk_document` FOREIGN KEY (`document_id`) REFERENCES `knowledge_document` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 5. 接口文档总览（按 Controller）

### 5.1 认证模块 `/api/auth`

- `POST /register` 注册
- `POST /login` 登录
- `POST /logout` 退出
- `GET /me` 当前用户
- `GET /profile-summary` 个人摘要
- `POST /avatar` 上传头像

### 5.2 智能问答模块 `/api/chat`

- `POST /send` 发送消息（SSE）
- `GET /conversations` 对话列表
- `GET /conversations/{id}/messages` 会话消息
- `DELETE /conversations/{id}` 删除会话
- `POST /feedback` 回答反馈
- `GET /retrieval-metrics` 检索评估指标
- `POST /retrieval-metrics/reset` 重置检索评估

### 5.3 知识文档模块 `/api/documents`

- `GET /` 文档列表
- `POST /` 新增文档
- `DELETE /{id}` 删除文档
- `GET /{id}/related-nodes` 文档关联图谱节点

### 5.4 知识图谱模块 `/api/graph`

- `GET /overview` 图谱总览
- `GET /node/{id}` 或 `/nodes/{id}` 节点详情
- `GET /node/{id}/neighbors` 或 `/nodes/{id}/neighbors` 邻居展开
- `GET /search` 图谱搜索
- `GET /health` 图谱健康度
- `GET /node/{id}/related-documents` 或 `/nodes/{id}/related-documents` 关联文档

### 5.5 学习路径模块 `/api/path`

- `POST /generate` 生成路径
- `GET /list` 路径列表
- `PUT /node/{id}/status` 更新节点状态
- `DELETE /{pathId}` 删除路径

### 5.6 学习分析模块 `/api/progress`

- `GET /dashboard` 仪表盘
- `GET /smart-insights` 智能洞察
- `POST /weekly-plan/toggle` 周计划勾选
- `GET /heatmap` 热力图
- `GET /radar` 雷达图
- `GET /report` PDF 报告导出

### 5.7 代码片段模块 `/api/snippets`

- `POST /` 新建
- `GET /` 列表查询
- `GET /recommend` 推荐
- `PUT /{id}` 更新
- `DELETE /{id}` 删除
- `POST /{id}/use` 标记使用
- `POST /{id}/feedback` 片段反馈
- `GET /export` 导出 JSON
- `POST /import` 导入 JSON

---

## 6. 典型请求示例

### 6.1 登录

```json
{
  "username": "zhangsan",
  "password": "12345678"
}
```

### 6.2 智能问答发送（SSE）

```json
{
  "conversationId": 1,
  "content": "解释 JavaScript 中的闭包"
}
```

### 6.3 生成学习路径

```json
{
  "target": "掌握 Spring Boot 微服务开发",
  "knownKnowledgeIds": ["java", "sql"]
}
```

### 6.4 新建代码片段

```json
{
  "title": "Vue3 防抖 Hook",
  "code": "export function useDebounce(){}",
  "language": "typescript",
  "description": "输入防抖封装",
  "tags": ["vue3", "hook"]
}
```

### 6.5 新增知识文档

```json
{
  "title": "Redis 缓存策略实践",
  "content": "缓存穿透、击穿、雪崩处理方案...",
  "category": "redis"
}
```

---

## 7. 统一响应结构（系统当前实现）

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "error": {
    "bizCode": "VALIDATION_FAILED",
    "details": []
  },
  "traceId": "c8f3...",
  "timestamp": "2026-04-20T13:00:00Z",
  "path": "/api/xxx"
}
```

说明：

- 成功：`code=200`，`message=success`
- 失败：返回标准业务错误结构，便于前后端统一处理

---

## 8. 部署与运行（README 关键信息）

### 8.1 本地开发启动

后端：

```bash
cd backend
mvn spring-boot:run
```

前端：

```bash
cd frontend
npm install
npm run dev
```

访问地址：

- 前端开发：`http://localhost:5173`
- 后端接口：`http://localhost:8080`
- Elasticsearch：`http://localhost:19200`
- Neo4j Browser：`http://localhost:7474`

### 8.2 Docker Compose 一键启动

```bash
docker compose up -d
```

编排服务：

- MySQL（3306）
- Neo4j（7474/7687）
- Redis（6379）
- Elasticsearch（19200 映射到容器 9200）
- Backend（8080，供 Nginx 反代）
- Frontend 构建容器
- Nginx（80）

---

## 9. 可提交材料建议（按课设平台上传）

建议将以下内容一起上传，平台可自动解析：

- 本文件：`课设提交-项目整合说明.md`
- 数据库脚本：`backend/src/main/resources/schema.sql`
- 部署文档：`部署文档.md`
- API 契约文档：`docs/api-contract-and-naming-guide.md`
- 需求规格：`SPEC.md`
- 项目源码压缩包（`backend + frontend + docker + docs`）

---

## 10. 补充说明

- 项目已落地 ECharts 图谱可视化（`frontend/src/components/graph/GraphRenderer.vue`）
- RAG 检索链路已包含 chunk 级语义检索、关键词融合、检索评估统计
- 开发环境日志字符集建议统一，避免 Windows 控制台出现乱码（当前 `application-dev.yml` 控制台编码为 `GBK`）

